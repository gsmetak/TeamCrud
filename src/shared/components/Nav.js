import styled from 'styled-components'
import Theme from '../../theme'

const Nav = styled.nav`
  background: linear-gradient(90deg, ${Theme.mainColor} 0%, ${Theme.secondaryColor} 66.15%);
`

export default Nav
